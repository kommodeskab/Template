_templates_suffix: ""

_exclude:
    - .github          # template repo's own CI — not for generated projects
    - uv.lock          # generated — user runs `uv sync` after copy
    - copier.yml

project_name:
    type: str
    help: "Python package name (snake_case, e.g. my_project)"
